<UserControl x:Class="NotedUI.UI.Settings.About"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="500"
             d:DesignWidth="700"
             Background="{StaticResource NoteBackgroundBrush}"
             mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border HorizontalAlignment="Center" CornerRadius="70" Margin="0,20,0,0"
              Background="{StaticResource AppTopBannerBrush}">
            <Image Width="300"
                   Margin="35,20,25,20"
                   RenderOptions.BitmapScalingMode="HighQuality"
                   Source="..\..\Resources\Graphics\Banner.png" />
        </Border>

        <UniformGrid Grid.Row="2"
                     Columns="1" />
    </Grid>

</UserControl>
